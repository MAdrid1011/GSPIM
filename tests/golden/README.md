# Golden Fixture Results

These versioned values are the independently reviewed expected outputs for the
five-frame Explicit4D, Ex4DGS, and Anchored fixtures, plus the six-frame
Explicit4D window-decision and loopback fixture. Tests compare the
implementation, independent oracle, and each manifest so a shared
implementation regression cannot update expected mechanism state silently.
